# Canonical Debian configuration for build images
# DEBIAN_CODENAME is used for upstream language/runtime images (e.g., golang:1.26-trixie)
# DISTROLESS_TAG controls the suffix for distroless base images (e.g., gcr.io/distroless/static-debian13)
DEBIAN_CODENAME=trixie
DISTROLESS_TAG=debian13
